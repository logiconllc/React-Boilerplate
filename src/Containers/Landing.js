import * as React from 'react';
import Header from '../Components/Header';

 const Landing = props => {
    return (
        <div>
            <Header />
        </div>
    );
};

export default Landing